a simple todo app to play around with kotlin, still don't fully understand views, layouts, adapters??, paintFlags. will look into persisting the data with a database, maybe firestore!

also.. the UI is too ugly
